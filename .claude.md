# EnergyPlus Automation Tool - Claude Context

**Projekt**: Automatisiertes Gebäudemodellierungs- und Simulationstool für EnergyPlus
**Standort**: Salzburg, Österreich
**Erstellungsdatum**: 10.11.2025
**Letztes Update**: 10.11.2025

---

## 🎯 PROJEKT-ZIEL

Entwicklung eines Python-basierten Tools zur:
- Automatischen Erstellung von EnergyPlus-Gebäudemodellen
- Durchführung von Simulationen (Standard, Co-Simulation, HIL)
- Auswertung und Optimierung von Gebäudeenergiebedarfen
- Integration mit datengetriebenen Modellen (ML)

---

## 📁 PROJEKT-STRUKTUR

```
AI_BS/
├── .claude.md                    # DIESE DATEI - Kontext für Claude
├── README.md                     # Hauptdokumentation
├── STATUS.md                     # Aktueller technischer Status
├── WAS_IST_MÖGLICH.md           # Feature-Übersicht
├── LIVE_DEMO_ERGEBNISSE.md      # Beispiel-Ergebnisse
├── INSTALLATION_WSL.md          # WSL-Installation Guide
├── QUICK_REFERENCE.md           # Schnellreferenz
│
├── config/
│   └── default_config.yaml      # Hauptkonfiguration
│
├── src/
│   ├── geometry/
│   │   └── simple_box.py        # ⚠️ BUGGY - API-Inkompatibilität
│   ├── materials/
│   │   └── standard_constructions.py  # ✅ Funktioniert
│   ├── simulation/
│   │   └── runner.py            # ✅ Funktioniert (Single & Batch)
│   └── utils/
│       └── config.py            # ✅ Konfigurationsmanagement
│
├── examples/
│   ├── 01_simple_box_simulation.py  # ⚠️ Benötigt Fixes
│   └── 02_batch_simulation.py       # ⚠️ Benötigt Fixes
│
├── tests/
│   ├── test_geometry.py
│   └── test_config.py
│
├── data/
│   └── weather/
│       └── example.epw          # ✅ Salzburg-Wetterdaten
│
├── output/                      # Simulationsergebnisse
│
├── quick_start.py               # ✅ FUNKTIONIERENDES Minimal-Beispiel
├── demo_salzburg.py             # ⚠️ In Entwicklung
├── beispiel_salzburg_analyse.py # ✅ SQL-Analyse-Beispiel
└── requirements.txt             # Python-Dependencies
```

---

## ✅ WAS FUNKTIONIERT (STAND 10.11.2025)

### 1. **EnergyPlus-Installation**
- **Version**: 23.2.0-7636e6b3e9
- **Pfad**: `/home/luma/EnergyPlus-23.2.0-7636e6b3e9-Linux-Ubuntu22.04-x86_64/`
- **Executable**: `~/EnergyPlus-.../energyplus`
- **Status**: ✅ Vollständig funktionsfähig

### 2. **Python-Umgebung**
- **Python**: 3.10.12
- **Virtual Env**: `venv/`
- **Key Packages**: eppy 0.5.63, pandas, pydantic
- **Status**: ✅ Installiert und getestet

### 3. **Simulationen**
```bash
# Funktioniert:
~/EnergyPlus-.../energyplus \
  -w data/weather/example.epw \
  -d output/results \
  gebäude.idf
```
- **Status**: ✅ Produktionsreif
- **Getestet**: Mit Salzburg-Wetter, verschiedene Gebäude
- **Performance**: ~2-5 Sekunden für Jahressimulation

### 4. **IDF-Manipulation mit eppy**
```python
from eppy.modeleditor import IDF

IDF.setiddname("/home/luma/EnergyPlus-.../Energy+.idd")
idf = IDF("gebäude.idf")

# Objekte hinzufügen/ändern
zone = idf.newidfobject('ZONE')
zone.Name = "MeinRaum"

idf.saveas("modified.idf", encoding='utf-8')
```
- **Status**: ✅ Funktioniert
- **Wichtig**: Immer `encoding='utf-8'` verwenden für Umlaute!

### 5. **Verfügbare Ressourcen**
- **Wetterdaten**: ✅ Salzburg (data/weather/example.epw)
- **Beispiel-Gebäude**: ✅ 100+ in `~/EnergyPlus-.../ExampleFiles/`
- **Dokumentation**: ✅ Umfassend (siehe README.md)

---

## ⚠️ BEKANNTE PROBLEME

### 1. **src/geometry/simple_box.py - NICHT FUNKTIONSFÄHIG**

**Problem**: API-Inkompatibilität zwischen eppy und EnergyPlus 23.2

**Details**:
- Feldnamen haben sich in EnergyPlus 23.2 geändert
- Z.B.: `Zone_Name` → `Zone_or_ZoneList_or_Space_or_SpaceList_Name`
- `newidfobject()` ohne Template-IDF funktioniert nicht

**Symptome**:
```python
AttributeError: 'IDF' object has no attribute 'model'
# ODER
unknown field Zone_Name
```

**Workaround**:
```python
# NICHT funktioniert:
idf = IDF()  # Leeres IDF

# FUNKTIONIERT:
# 1. Template-IDF erstellen
template = "VERSION, 23.2;\n"
with open("/tmp/template.idf", "w") as f:
    f.write(template)

# 2. Template laden
idf = IDF("/tmp/template.idf")

# 3. Jetzt können Objekte hinzugefügt werden
```

**Status**: 🔧 Benötigt Refactoring
**Priorität**: Mittel (Workaround verfügbar)

### 2. **Fenster-Generierung**

**Problem**: Vertex-Berechnung für FENESTRATIONSURFACE:DETAILED nicht implementiert

**Status**: ⚠️ Deaktiviert (siehe simple_box.py Zeile 360-362)
**Workaround**: Beispiel-IDFs mit Fenstern als Basis verwenden

### 3. **Encoding-Probleme mit Umlauten**

**Problem**: Default encoding in eppy ist 'latin-1'

**Lösung**: Immer explizit setzen:
```python
idf.saveas("datei.idf", encoding='utf-8')
```

---

## 🔧 WICHTIGE PFADE & BEFEHLE

### Aktivierung der Umgebung
```bash
cd /mnt/c/Users/lugma/source/repos/AI_BS
source venv/bin/activate
```

### EnergyPlus ausführen
```bash
~/EnergyPlus-23.2.0-7636e6b3e9-Linux-Ubuntu22.04-x86_64/energyplus \
  -w data/weather/example.epw \
  -d output/results \
  input.idf
```

### IDD-Datei (wichtig für eppy)
```python
IDD_PATH = "/home/luma/EnergyPlus-23.2.0-7636e6b3e9-Linux-Ubuntu22.04-x86_64/Energy+.idd"
```

### Beispiel-Dateien
```bash
EXAMPLES_DIR = "~/EnergyPlus-23.2.0-7636e6b3e9-Linux-Ubuntu22.04-x86_64/ExampleFiles/"
```

---

## 📝 TYPISCHE WORKFLOWS

### Workflow 1: Bestehendes Gebäude simulieren
```bash
# 1. Beispiel kopieren
cp ~/EnergyPlus-.../ExampleFiles/RefBldgSmallOffice.idf mein_projekt.idf

# 2. Simulieren
~/EnergyPlus-.../energyplus -w data/weather/example.epw -d output/results mein_projekt.idf

# 3. Ergebnisse anschauen
explorer.exe output/results/eplustbl.htm
```

### Workflow 2: Gebäude mit Python anpassen
```python
from eppy.modeleditor import IDF
from pathlib import Path

# Setup
IDF.setiddname("/home/luma/EnergyPlus-.../Energy+.idd")

# Laden
idf = IDF("mein_projekt.idf")

# Anpassen (siehe MANUAL_GEBAEUDE_ANPASSUNG.md für Details)
building = idf.idfobjects['BUILDING'][0]
building.Name = "Mein Gebäude Salzburg"

# Speichern
idf.saveas("modified.idf", encoding='utf-8')
```

### Workflow 3: Parameterstudie
```python
# Siehe beispiel_parameterstudie.py (falls vorhanden)
# Oder erstellen als nächstes!
```

---

## 🚀 NÄCHSTE ENTWICKLUNGSSCHRITTE

### Kurzfristig (Priorität HOCH)
1. **simple_box.py reparieren**
   - Feldnamen für EnergyPlus 23.2 aktualisieren
   - Template-basierte IDF-Erstellung implementieren
   - Unit-Tests schreiben

2. **Beispiel-Skripte funktionsfähig machen**
   - 01_simple_box_simulation.py debuggen
   - 02_batch_simulation.py testen

3. **Parameterstudie-Beispiel erstellen**
   - Z.B. Dämmstärke-Variation
   - Automatische Ergebnis-Vergleich

### Mittelfristig
4. **Post-Processing-Modul**
   - SQL-Datenbank-Parser
   - Kennzahlen-Extraktion
   - Automatische Grafiken (matplotlib)

5. **TABULA-Integration**
   - Deutsche Gebäudetypologie
   - Automatische Material-Auswahl

6. **Batch-Processing testen**
   - Parallelisierung verifizieren
   - Performance-Optimierung

### Langfristig
7. **Co-Simulation (FMI/FMU)**
8. **Hardware-in-the-Loop**
9. **ML-Integration**
10. **Web-GUI**

---

## 🐛 DEBUGGING-TIPPS

### Problem: Simulation schlägt fehl
```bash
# Fehlerlog anschauen
cat output/results/eplusout.err | grep "Severe"
```

### Problem: eppy-Fehler
```python
# IDF-Initialisierung prüfen
print(f"Has model: {hasattr(idf, 'model')}")

# Verfügbare Objekt-Typen
print(idf.idfobjects.keys())
```

### Problem: Feldname unbekannt
```python
# Objektfelder inspizieren
obj = idf.newidfobject('ZONE')
print([f for f in dir(obj) if not f.startswith('_')])
```

### Problem: Encoding-Fehler
```python
# Immer UTF-8 verwenden
idf.saveas("file.idf", encoding='utf-8')
```

---

## 📊 BEISPIEL-NUTZUNG

### Live-Demo (funktioniert!)
```bash
# Im Projekt-Root:
source venv/bin/activate

# Minimal-Beispiel
python quick_start.py

# Vollständige Simulation
cp ~/EnergyPlus-.../ExampleFiles/RefBldgSmallOfficeNew2004_Chicago.idf demo.idf
~/EnergyPlus-.../energyplus -w data/weather/example.epw -d output/demo demo.idf
explorer.exe output/demo/eplustbl.htm
```

---

## 🔑 WICHTIGE ERKENNTNISSE

### Was funktioniert GUT:
- ✅ EnergyPlus-Simulationen (sehr stabil)
- ✅ IDF-Manipulation mit eppy (mit Vorsicht)
- ✅ Beispiel-Dateien als Basis verwenden
- ✅ Ergebnis-Analyse über HTML-Reports

### Was SCHWIERIG ist:
- ⚠️ Vollautomatische Geometrie-Generierung
- ⚠️ Feldnamen-Kompatibilität zwischen Versionen
- ⚠️ Komplexe HVAC-Systeme (viele Abhängigkeiten)

### Empfohlener Ansatz:
1. **Beispiel-IDF als Basis** verwenden
2. **Mit eppy parametrisch anpassen**
3. **Simulieren & Ergebnisse analysieren**
4. **Iterativ verbessern**

---

## 📚 DOKUMENTATIONS-QUELLEN

### 🌟 VOLLSTÄNDIGE SYSTEMDOKUMENTATION (NEU - 2025-11-14)

**⚠️ WICHTIG: Diese Dokumentation MUSS bei Code-Änderungen aktualisiert werden!**

**Haupt-Einstieg:**
- **`SYSTEM_DOKUMENTATION.md`** - Übersicht & Index zu allen Dokumentationen

**Detail-Dokumentationen (docs/):**

1. **`docs/01_WEB_UI_DOKUMENTATION.md`** (1.482 Zeilen, 41K)
   - Alle 4 Streamlit-Seiten (Geometrie, HVAC, Simulation, Ergebnisse)
   - Session State Variablen & Datenfluss
   - Input-Widgets & Backend-Funktionen
   - **Wann nutzen:** UI-Änderungen, neue Seiten, Session State Debug

2. **`docs/02_CORE_MODULE_DOKUMENTATION.md`** (1.172 Zeilen, 28K)
   - `building_model.py`, `climate_data.py`, `config.py`, `simulation_config.py`, `materialien.py`
   - Factory Methods, Pydantic Models, YAML-Handling
   - **Wann nutzen:** Backend-Entwicklung, neue Core-Features

3. **`docs/03_FEATURES_DOKUMENTATION.md`** (1.088 Zeilen, 24K)
   - Geometrie: five_zone_generator, geometry_solver, perimeter_calculator
   - HVAC: ideal_loads, HVACTemplate-System
   - Auswertung: sql_parser, kpi_rechner, visualisierung
   - **Wann nutzen:** Feature-Erweiterungen, neue Geometrie-Modi, neue KPIs

4. **`docs/04_DATENFLUSS_DOKUMENTATION.md`** (815 Zeilen, 20K)
   - SimpleBox-Workflow (Schritt-für-Schritt)
   - Energieausweis-Workflow (Schritt-für-Schritt)
   - Datentransformationen & Session State Dependencies
   - **Wann nutzen:** Gesamtverständnis, Debugging, neue Workflows

5. **`docs/05_IDF_STRUKTUR_DOKUMENTATION.md`** (901 Zeilen, 23K)
   - Parameter-Mapping: Energieausweis → IDF
   - **KRITISCH:** Vertex-Ordering Konventionen (REVERSED für Floors!)
   - Boundary Objects (Inter-Zone Walls paarweise)
   - Vollständiges IDF-Beispiel
   - **Wann nutzen:** IDF-Generator-Entwicklung, Surface-Bugs

6. **`docs/06_SIMULATION_DOKUMENTATION.md`** (820 Zeilen, 21K)
   - Simulations-Workflow (EnergyPlusRunner, ExpandObjects)
   - **Aktuell genutzt:** Nur 11 von 200+ Variablen (5%!)
   - **Verfügbar aber ungenutzt:** PMV/PPD, Zonale Daten, Tabular Reports
   - Ungenutztes Potential & Empfehlungen
   - **Wann nutzen:** Neue KPIs, Output-Variablen erweitern

**Gesamt:** ~6.900 Zeilen, 168 KB umfassende Dokumentation

**📝 Wartungsregel:**
- Bei jeder Code-Änderung: Betroffene Dokumentation aktualisieren!
- Bei neuen Features: Relevanten Abschnitt erweitern
- Bei Datenfluss-Änderungen: 04_DATENFLUSS aktualisieren
- Bei IDF-Änderungen: 05_IDF_STRUKTUR aktualisieren

---

### Intern (bisherige Dokumentation):
- `README.md` - Vollständige Projektdokumentation
- `WAS_IST_MÖGLICH.md` - Feature-Übersicht
- `STATUS.md` - Technischer Status
- `LIVE_DEMO_ERGEBNISSE.md` - Beispiel-Workflow

### Extern:
- **EnergyPlus Docs**: https://energyplus.net/documentation
- **eppy Docs**: https://eppy.readthedocs.io/
- **Wetterdaten**: https://energyplus.net/weather

### Beispiele:
- `~/EnergyPlus-.../ExampleFiles/` - 100+ IDF-Dateien
- `~/EnergyPlus-.../DataSets/` - Material-Datenbanken

---

## 🤖 FÜR CLAUDE/KI-ASSISTENTEN

### Bei neuer Session:
1. **ZUERST:** Lies diese Datei (`.claude.md`)
2. **DANN:** Lies `SYSTEM_DOKUMENTATION.md` für Gesamtüberblick
3. **Bei Code-Änderungen:** Lies relevante Detail-Dokumentation (docs/01-06)
4. Prüfe `STATUS.md` für aktuelle Probleme
5. Schau in `WAS_IST_MÖGLICH.md` für Features

### ⚠️ VOR Code-Änderungen:
- **IMMER** relevante Dokumentation (docs/01-06) lesen!
- Verhindert Breaking Changes
- Zeigt Zusammenhänge und Dependencies
- Dokumentiert kritische Konventionen (z.B. Vertex-Ordering!)

### ✅ NACH Code-Änderungen:
- **IMMER** betroffene Dokumentation aktualisieren!
- Bei UI-Änderungen: `docs/01_WEB_UI_DOKUMENTATION.md`
- Bei Backend-Änderungen: `docs/02_CORE_MODULE_DOKUMENTATION.md`
- Bei Features: `docs/03_FEATURES_DOKUMENTATION.md`
- Bei Workflow-Änderungen: `docs/04_DATENFLUSS_DOKUMENTATION.md`
- Bei IDF-Änderungen: `docs/05_IDF_STRUKTUR_DOKUMENTATION.md`
- Bei neuen Output-Variablen: `docs/06_SIMULATION_DOKUMENTATION.md`

### Bekannte Bugs zu vermeiden:
- ❌ NICHT `IDF()` ohne Template verwenden
- ❌ NICHT ohne `encoding='utf-8'` speichern
- ❌ NICHT Feldnamen aus EnergyPlus 22.x verwenden

### Best Practices:
- ✅ Immer Beispiel-IDFs als Basis
- ✅ Encoding explizit setzen
- ✅ Feldnamen mit `dir(obj)` prüfen
- ✅ Templates für IDF-Erstellung nutzen

### 📝 Dokumentations-Richtlinien (WICHTIG!)

**Jedes Feature MUSS eine README.md haben!**

Bei Arbeit an Features **IMMER**:

1. **Feature-Level README.md erstellen/aktualisieren**
   ```
   features/<feature_name>/README.md
   ```
   - Architektur-Übersicht (Layer-Struktur)
   - Alle Module und deren Zweck dokumentieren
   - API-Beispiele mit Code
   - Datenfluss-Diagramme
   - Testing-Strategie
   - Metriken (Zeilen, Komponenten, etc.)

2. **Unterordner-Dokumentation**
   ```
   features/<feature_name>/<subdir>/README.md
   ```
   - Jede Komponente detailliert beschreiben
   - Verwendungsbeispiele
   - Interne Algorithmen erklären
   - Abhängigkeiten dokumentieren

3. **Haupt-Features README**
   ```
   features/README.md
   ```
   - Übersicht aller Features
   - Querverweise zwischen Features
   - Shared Components

**Ziel**: Nachhaltige Dokumentation, Nachvollziehbarkeit, Prozesssicherheit

**Wann dokumentieren**:
- ✅ Bei neuem Feature-Erstellen
- ✅ Nach größeren Refactorings
- ✅ Nach Architektur-Änderungen
- ✅ Wenn neue Komponenten hinzukommen

**Format**:
- GitHub-Flavored Markdown
- Emojis für visuelle Struktur
- Code-Beispiele mit Syntax-Highlighting
- Diagramme in ASCII oder Mermaid
- Metriken in Tabellen

**Beispiel**: `features/geometrie/README.md` (500+ Zeilen, vollständig)
- ✅ 4-Layer Architektur erklärt
- ✅ Alle Komponenten dokumentiert
- ✅ Workflow-Beispiele
- ✅ Testing-Strategie
- ✅ Metriken & Roadmap

### Code-Patterns, die funktionieren:
```python
# IDF initialisieren
import tempfile
from eppy.modeleditor import IDF
from pathlib import Path

IDF.setiddname("/home/luma/EnergyPlus-.../Energy+.idd")

template = "VERSION, 23.2;\n"
with tempfile.NamedTemporaryFile(mode='w', suffix='.idf', delete=False) as f:
    f.write(template)
    temp_path = f.name

idf = IDF(temp_path)
Path(temp_path).unlink()

# Jetzt kann man arbeiten
zone = idf.newidfobject('ZONE')
zone.Name = "Test"
# ...

idf.saveas("output.idf", encoding='utf-8')
```

---

## 🔄 UPDATE-LOG

### 2025-11-14 - Tabular Reports Feature implementiert 🚀
- ✅ **NEUES FEATURE**: Tabular Reports Parser (Priorität 1 Quick Win)
  - **Neues Modul**: `features/auswertung/tabular_reports.py` (395 Zeilen)
    - `TabularReportParser` Klasse für vorgefertigte EnergyPlus Summary Reports
    - 4 Datenklassen: `EndUseSummary`, `SiteSourceEnergy`, `HVACSizing`, `EnvelopePerformance`
    - SQL-Query mit JOINs zur Auflösung von String-Indices
  - **Integration in bestehende Module:**
    - `sql_parser.py`: 3 neue Methoden für Tabular Data (+44 Zeilen)
    - `kpi_rechner.py`: `ErweiterteKennzahlen` Dataclass, `berechne_erweiterte_kennzahlen()` (+60 Zeilen)
    - `visualisierung.py`: 4 neue Visualisierungsfunktionen (+336 Zeilen)
    - `04_Ergebnisse.py`: Neuer Sub-Tab "📈 Tabular Reports (Erweitert)" (+202 Zeilen)
  - **Vorteile:**
    - ✅ Instant-Zugriff auf aggregierte Daten (keine 8760-Werte Summierung!)
    - ✅ 95% ungenutztes EnergyPlus-Potential jetzt verfügbar
    - ✅ Detaillierte End-Use Breakdown (inkl. Fans, Pumps)
    - ✅ Primärenergie-Analyse (Site vs. Source Energy)
    - ✅ HVAC Design Loads mit Auslegungstag
  - **Total**: ~1.080 Zeilen neuer Code

- 🐛 **GEFUNDENE FEHLER** (durch Tabular Reports aufgedeckt):
  - **Issue**: Design Loads sind 0 → IDF-Generierung oder Output:Variables Problem
  - **Issue**: Interne Lasten (Lights/Equipment) sehr hoch angesetzt → Werte überprüfen
  - Diese Fehler waren vorher nicht sichtbar, da nur aggregierte Zeitreihen genutzt wurden!

- 📊 **CHANGELOG.md aktualisiert:**
  - Vollständige Feature-Dokumentation
  - Technische Details und Vorteile
  - Geänderte Dateien dokumentiert

### 2025-11-14 - Vollständige Systemdokumentation erstellt ⭐
- ✅ **UMFASSENDE DOKUMENTATION**: 7 Dokumente, ~6.900 Zeilen, 168 KB
  - `SYSTEM_DOKUMENTATION.md` - Haupt-Index & Übersicht
  - `docs/01_WEB_UI_DOKUMENTATION.md` - Alle 4 Streamlit-Seiten (1.482 Zeilen)
  - `docs/02_CORE_MODULE_DOKUMENTATION.md` - Backend-Module (1.172 Zeilen)
  - `docs/03_FEATURES_DOKUMENTATION.md` - Geometrie, HVAC, Auswertung (1.088 Zeilen)
  - `docs/04_DATENFLUSS_DOKUMENTATION.md` - Workflows Ende-zu-Ende (815 Zeilen)
  - `docs/05_IDF_STRUKTUR_DOKUMENTATION.md` - Parameter-Mapping & Konventionen (901 Zeilen)
  - `docs/06_SIMULATION_DOKUMENTATION.md` - EnergyPlus Output-Analyse (820 Zeilen)
  - `docs/README.md` - Dokumentations-Übersicht

- 📊 **Wichtigste Erkenntnisse:**
  - **Datenfluss:** Vollständig von User Input → IDF → Simulation → KPIs dokumentiert
  - **Ungenutztes Potential:** 95% der EnergyPlus-Daten werden NICHT genutzt!
  - **Kritische Konventionen:** Vertex-Ordering, Boundary Objects, eppy Bugs dokumentiert
  - **Parameter-Mapping:** Energieausweis → IDF vollständig nachvollziehbar

- ✅ **`.claude.md` aktualisiert:**
  - Neue Sektion "VOLLSTÄNDIGE SYSTEMDOKUMENTATION"
  - Wartungsregeln für Dokumentations-Pflege
  - "Wann nutzen"-Hinweise für jedes Dokument

- 🎯 **Zweck:**
  - AI-Assistenten: Präziser Kontext für Code-Änderungen
  - Entwickler: Gesamtverständnis & Debugging
  - Wartung: Identifikation von überflüssigen Daten/Files
  - Nachvollziehbarkeit: Von Anfang bis Ende dokumentiert

### 2025-11-13 - Phase 3 Refactoring Complete
- ✅ **FiveZoneGenerator Phase 3**: SurfaceGenerator extrahiert
  - Neue Komponente: `components/surfaces.py` (612 Zeilen)
  - Orchestrator reduziert: 1379 → 520 Zeilen (-62%)
  - Alle 12 Integration-Tests bestanden
- ✅ **Dokumentation**: Umfassende READMEs aktualisiert
  - `features/geometrie/README.md` (Phase 3 markiert)
  - `components/README.md` (SurfaceGenerator dokumentiert)
- ✅ **Dokumentations-Richtlinien** in `.claude.md` ergänzt
  - Feature-Level README.md Pflicht
  - Unterordner-Dokumentation
  - Nachhaltige Prozesssicherheit
- 📊 **Metriken**: 5 spezialisierte Komponenten + Orchestrator
  - EppyBugFixer, MetadataGenerator, ZoneGenerator, MaterialsGenerator, SurfaceGenerator

### 2025-11-10 - Initiale Erstellung
- Projekt aufgesetzt
- EnergyPlus 23.2.0 installiert (WSL2)
- Python-Umgebung konfiguriert
- Live-Demo erfolgreich durchgeführt
- Bekannte Probleme dokumentiert
- `.claude.md` erstellt

### Nächstes Update:
- [x] ~~Tabular Reports parsen~~ ✅ **DONE 2025-11-14**
- [ ] **BUGFIXES (durch Tabular Reports aufgedeckt):**
  - [ ] Design Loads Problem beheben (aktuell = 0)
  - [ ] Interne Lasten (Lights/Equipment) Werte überprüfen und korrigieren
- [ ] Phase 4: Return Types Enhancement (SurfaceInfo, WindowInfo)
- [ ] Phase 4: U-Value Construction Generator
- [ ] Zonale Auswertung implementieren (Nord/Süd-Vergleich)
- [ ] PMV/PPD Komfort-Metriken aktivieren
- [ ] Parameterstudie-Beispiel erstellt
- [ ] Post-Processing-Modul erweitern

---

## 💡 QUICK COMMANDS

```bash
# Projekt-Setup
cd /mnt/c/Users/lugma/source/repos/AI_BS
source venv/bin/activate

# Test ausführen
python quick_start.py

# Simulation
~/EnergyPlus-23.2.0-7636e6b3e9-Linux-Ubuntu22.04-x86_64/energyplus \
  -w data/weather/example.epw -d output/test input.idf

# Ergebnisse öffnen
explorer.exe output/test/eplustbl.htm

# Tests
pytest tests/

# Code-Formatierung
black src/ tests/ examples/
```

---

**Letztes Update**: 2025-11-14 (Tabular Reports Feature + Bugfixes dokumentiert)
**Nächste Priorität**: Design Loads Bugfix, Interne Lasten prüfen
**Status**: 🟢 Tabular Reports live! | 🐛 Fehler durch neue Daten aufgedeckt (Design Loads = 0, Interne Lasten zu hoch)
